 /*  const mysql = require("mysql");
 const firstreactbase = require("../config/database");

 const User = db.define("user", {
  firstName: {
    type: mysql.STRING,
    required: true,
  },
  lastName: {
    type: mysql.STRING,
    required: true,
  },
  email: {
    type: mysql.STRING,
    required: true,
  },
   password: {
    type: mysql.STRING,
    required: true,
  },
});

 module.exports = User; */
